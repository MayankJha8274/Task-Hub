<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/theme.css">
</head>

<body class="bg-light">

  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container-fluid px-4">
      <a class="navbar-brand fw-bold" href="/dashboard">
        <i class="bi bi-check2-square"></i> TaskHub
      </a>
      
      <!-- Navigation Links -->
      <div class="navbar-nav me-auto">
        <a class="nav-link active" href="/dashboard">
          <i class="bi bi-list-task"></i> Tasks
        </a>
        <a class="nav-link" href="/notes">
          <i class="bi bi-journal-text"></i> Notes
        </a>
      </div>
      
      <!-- User Controls -->
      <div class="d-flex align-items-center gap-3">
        <button id="themeToggle" class="btn btn-sm" style="background: transparent; border: none; color: #fff;">
          <i id="themeIcon" class="bi bi-moon-stars fs-5"></i>
        </button>
        <div class="dropdown">
          <button class="btn btn-outline-light btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
            <i class="bi bi-person-circle"></i> <%= user.name %>
          </button>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item text-danger" href="/auth/logout"><i class="bi bi-box-arrow-right"></i> Logout</a></li>
          </ul>
        </div>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h2>Your Tasks</h2>

      <div class="d-flex gap-2 align-items-center">
        <select id="categoryFilter" class="form-select me-2" style="width:200px" onchange="applyFilters()">
          <% categories.forEach(cat => { %>
            <option value="<%= cat %>" <%= selectedCategory === cat ? 'selected' : '' %>>
              <%= cat === 'all' ? 'All Categories' : cat %>
            </option>
          <% }) %>
        </select>

        <select id="sortFilter" class="form-select" style="width:180px" onchange="applyFilters()">
          <option value="created_desc" <%= selectedSort === 'created_desc' ? 'selected' : '' %>>Newest</option>
          <option value="due_asc" <%= selectedSort === 'due_asc' ? 'selected' : '' %>>Due Date ↑</option>
          <option value="due_desc" <%= selectedSort === 'due_desc' ? 'selected' : '' %>>Due Date ↓</option>
        </select>
      </div>
    </div>

    <div class="card mb-4">
      <div class="card-body">
        <form action="/tasks/add" method="POST" class="row g-2">
          <div class="col-sm-4">
            <input type="text" name="title" class="form-control" placeholder="Task title" required>
          </div>

          <div class="col-sm-2">
            <input type="text" name="category" class="form-control" placeholder="Category">
          </div>

          <div class="col-sm-2">
            <input type="date" name="dueDate" class="form-control" placeholder="Due Date">
          </div>

          <div class="col-sm-2">
            <input type="datetime-local" name="reminder" class="form-control" title="Set Reminder">
          </div>

          <div class="col-sm-2">
            <button class="btn btn-dark w-100">Add Task</button>
          </div>
        </form>
      </div>
    </div>

    <div class="row g-3">
      <% if (tasks.length === 0) { %>
        <div class="col-12">
          <div class="alert alert-info">No tasks found.</div>
        </div>
      <% } %>

      <% tasks.forEach(task => { %>
        <div class="col-md-6">
          <div class="card shadow-sm">
            <div class="card-body d-flex justify-content-between align-items-start">
              <div>
                <h5 class="card-title <%= task.completed ? 'text-decoration-line-through text-muted' : '' %>">
                  <%= task.title %>
                </h5>

                <% if (task.category) { %>
                  <span class="badge bg-primary"><%= task.category %></span>
                <% } %>

                <% if (task.dueDate) { %>
                  <small class="text-muted d-block mt-1">
                    📅 Due: <%= new Date(task.dueDate).toLocaleDateString() %>
                  </small>
                <% } %>

                <% if (task.reminder && !task.reminderSent) { %>
                  <small class="text-warning d-block mt-1">
                    🔔 Reminder: <%= new Date(task.reminder).toLocaleString() %>
                  </small>
                <% } %>
                
                <% if (task.reminderSent) { %>
                  <small class="text-success d-block mt-1">
                    ✅ Reminder sent
                  </small>
                <% } %>

                <% if (task.description) { %>
                  <p class="mt-2 mb-0"><%= task.description %></p>
                <% } %>
              </div>

              <div class="btn-group-vertical ms-3">
                <% if (!task.completed) { %>
                  <form action="/tasks/<%= task._id %>/update" method="POST" class="mb-2">
                    <input type="hidden" name="completed" value="true">
                    <button class="btn btn-success btn-sm" type="submit">✔</button>
                  </form>
                <% } else { %>
                  <button class="btn btn-outline-success btn-sm mb-2" disabled>✔</button>
                <% } %>

                <form action="/tasks/<%= task._id %>/delete" method="POST">
                  <button class="btn btn-danger btn-sm" type="submit">🗑</button>
                </form>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <script>
    function applyFilters() {
      const cat = document.getElementById("categoryFilter").value;
      const sort = document.getElementById("sortFilter").value;

      const params = new URLSearchParams();
      
      if (cat && cat !== "all") params.set("category", cat);
      if (sort) params.set("sort", sort);

      window.location.href = "/dashboard" + (params.toString() ? "?" + params.toString() : "");
    }

    // Request notification permission when page loads
    if ("Notification" in window && Notification.permission === "default") {
      Notification.requestPermission().then(permission => {
        if (permission === "granted") {
          console.log("✅ Notification permission granted");
        }
      });
    }

    // Check for reminders every 30 seconds
    setInterval(checkReminders, 30000);
    checkReminders(); // Check immediately on page load

    async function checkReminders() {
      try {
        const response = await fetch('/tasks/check-reminders');
        const data = await response.json();
        
        if (data.reminders && data.reminders.length > 0) {
          data.reminders.forEach(task => {
            // Show browser notification
            if ("Notification" in window && Notification.permission === "granted") {
              new Notification("Task Reminder 🔔", {
                body: `Don't forget: ${task.title}`,
                icon: "/favicon.ico",
                tag: task._id, // Prevents duplicate notifications
                requireInteraction: true // Notification stays until dismissed
              });
            }
            
            // Log to console
            console.log(`🔔 Reminder: ${task.title}`);
          });
          
          // Reload page to update reminder status
          setTimeout(() => location.reload(), 2000);
        }
      } catch (err) {
        console.error("Error checking reminders:", err);
      }
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/theme.js"></script>

</body>
</html>


