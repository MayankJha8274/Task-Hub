<ul class="list-group" id="taskList">
  <% tasks.forEach(task => { %>
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <span class="<%= task.completed ? 'text-decoration-line-through text-muted' : '' %>">
        <%= task.title %>
      </span>

      <div class="btn-group">
        <% if (!task.completed) { %>
          <button class="btn btn-sm btn-success" onclick="completeTask('<%= task._id %>')">✔ Done</button>
        <% } %>

        <button class="btn btn-sm btn-danger" onclick="deleteTask('<%= task._id %>')">🗑 Delete</button>
      </div>
    </li>
  <% }) %>
</ul>

<script src="/js/dashboard.js"></script>


